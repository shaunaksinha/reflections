GIT COMMANDS

git diff <commit id 1> <commit id 2>
git diff --stat	: gives statistics related to commits
git log: gives shaid and other details relating to each commit
git checkout <commit id>: get to a snapshot of the rpo when this commit was made


[mod] git log --graph --oneline master coins

git diff: show changes that are unstaged now
git merge branch1 branch 2: merge branch2 onto branch1 (when you are on branch 1) 
